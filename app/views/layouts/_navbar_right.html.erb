<ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <% if current_user.profile.profile_pic.present? %>
              <%= image_tag current_user.profile.profile_pic, class: "profile-image img-circle" %>
          <% else %>
            <%= image_tag "nopicture.png", class: "profile-image img-circle" %>
          <% end %>
          <b class="caret"></b>
          </a>
        <ul class="dropdown-menu">
          <li><%= link_to 'Account Settings', edit_user_registration_path %></li>
          <li class="divider"></li>
          <li><%= link_to 'View Profile', myprofile_path %></li>
          <li class="divider"></li>
          <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
        </ul>
      </li>
    <% else %>
      <li><%= link_to 'Sign in', new_user_session_path %></li>
      <li><%= link_to 'Join', new_user_registration_path %></li>
    <% end %>
    <% if user_signed_in? %>
      <% if current_user.try(:admin?) %>
        <li><%= link_to 'DB', '/admin' %></li>
        <li><%= link_to 'Users', users_path %></li>
        <li><%= link_to 'CtPrefs', courtship_preferences_path %></li>
      <% end %>
    <% end %>
</ul>
